# VRT (Visual Regression Testing) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** SwiftUI Preview を PNG としてスナップショット化し、GitHub Actions Job Summary で3枚並列差分を表示する VRT を構築する。

**Architecture:** `swift-snapshot-testing` でテストコードから全 Preview を PNG に書き出す。main push 時に `vrt-baselines` orphan ブランチへ自動保存。PR 時はバックグラウンドで新規 PNG を生成し Python スクリプトで baseline と比較、差分を Job Summary に埋め込む。

**Tech Stack:** swift-snapshot-testing 1.17.x, Python 3 + Pillow, GitHub Actions (macos-26)

---

## File Structure

| ファイル | 操作 | 役割 |
|---|---|---|
| `TeloscopeTests/VRT/PreviewSnapshotTests.swift` | 新規作成 | 全 Preview の `assertSnapshot` テスト |
| `scripts/vrt-report.py` | 新規作成 | 差分計算・3枚並列 PNG 生成・Job Summary 書き出し |
| `.github/workflows/vrt.yml` | 新規作成 | PR チェック + main push 時 baseline 更新 |
| `.github/workflows/update-baseline.yml` | 新規作成 | `vrt-approved` ラベル時の baseline 更新 |

---

## Task 1: swift-snapshot-testing を Xcode プロジェクトに追加

**Files:**
- Modify: `Teloscope.xcodeproj` (Xcode GUI 操作)
- Modify: `Teloscope.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/Package.resolved`

- [ ] **Step 1: Xcode でパッケージを追加**

  Xcode を開いて `File > Add Package Dependencies...` を選択。  
  URL: `https://github.com/pointfreeco/swift-snapshot-testing`  
  Dependency Rule: **Up to Next Major Version** from `1.17.0`  
  Add to target: **TeloscopeTests**（`SnapshotTesting` product のみ選択）

- [ ] **Step 2: ビルドが通ることを確認**

  Run: `xcodebuild -scheme Teloscope build-for-testing 2>&1 | head -5`  
  Expected: `BUILD SUCCEEDED`（エラーなし）

- [ ] **Step 3: コミット**

  ```bash
  git add Teloscope.xcodeproj
  git commit -m "chore: add swift-snapshot-testing to TeloscopeTests"
  ```

---

## Task 2: vrt-baselines orphan ブランチを作成

**Files:** なし（Git 操作のみ）

- [ ] **Step 1: orphan ブランチを作成してリモートに push**

  ```bash
  git checkout --orphan vrt-baselines
  git rm -rf .
  echo "# VRT Baselines" > README.md
  git add README.md
  git commit -m "chore: initialize vrt-baselines branch"
  git push -u origin vrt-baselines
  git checkout main
  ```

  Expected: `vrt-baselines` ブランチが GitHub 上に存在すること。

---

## Task 3: PreviewSnapshotTests.swift を作成

**Files:**
- Create: `TeloscopeTests/VRT/PreviewSnapshotTests.swift`

- [ ] **Step 1: ファイルを作成**

  ```swift
  // SPDX-License-Identifier: MIT
  import AppKit
  import SnapshotTesting
  import SwiftData
  import SwiftUI
  import Testing
  @testable import Teloscope

  @Suite @MainActor struct PreviewSnapshotTests {
      // Fixed reference date for deterministic snapshots.
      // Using Date.now would produce different x-axis labels on every run.
      private let referenceDate = Date(timeIntervalSince1970: 1_700_000_000) // 2023-11-14

      private var record: SnapshotTestingConfiguration.Record {
          ProcessInfo.processInfo.environment["RECORD_SNAPSHOTS"] == "true" ? .all : .missing
      }

      // Wrap a SwiftUI view in NSHostingView sized to its intrinsic content size.
      private func snapshot<V: View>(_ view: V) -> NSHostingView<AnyView> {
          let hosting = NSHostingView(rootView: AnyView(view))
          hosting.frame = CGRect(origin: .zero, size: hosting.fittingSize)
          return hosting
      }

      // MARK: - PieWidgetView

      @Test func pieWidgetView_default() {
          withSnapshotTesting(record: record) {
              assertSnapshot(of: snapshot(
                  PieWidgetView(
                      title: "Approval Rate",
                      slices: [
                          PieSlice(label: "Approved (35)", value: 35, color: .green),
                          PieSlice(label: "Rejected (10)", value: 10, color: .red),
                      ],
                      centerLabel: "78%"
                  )
                  .frame(width: 260)
                  .padding()
              ), as: .image)
          }
      }

      @Test func pieWidgetView_noData() {
          withSnapshotTesting(record: record) {
              assertSnapshot(of: snapshot(
                  PieWidgetView(title: "Approval Rate", slices: [], centerLabel: nil)
                      .frame(width: 260)
                      .padding()
              ), as: .image)
          }
      }

      @Test func pieWidgetView_loading() {
          withSnapshotTesting(record: record) {
              assertSnapshot(of: snapshot(
                  PieWidgetView(title: "Approval Rate", slices: [], centerLabel: nil)
                      .redacted(reason: .placeholder)
                      .frame(width: 260)
                      .padding()
              ), as: .image)
          }
      }

      // MARK: - LineWidgetView

      // Computed from a fixed date to avoid non-deterministic x-axis labels.
      private var lineSampleDates: [Date] {
          (0..<24).map {
              Calendar.current.date(byAdding: .hour, value: $0,
                                    to: Calendar.current.startOfDay(for: referenceDate))!
          }
      }

      @Test func lineWidgetView_multiSeries() {
          let dates = lineSampleDates
          withSnapshotTesting(record: record) {
              assertSnapshot(of: snapshot(
                  LineWidgetView(
                      title: "Tokens Over Time",
                      series: [
                          LineSeries(label: "Input Tokens", color: .blue,
                                     dataPoints: dates.enumerated().map { i, d in (date: d, value: Double(i * 1200 + 800)) }),
                          LineSeries(label: "Output Tokens", color: .green,
                                     dataPoints: dates.enumerated().map { i, d in (date: d, value: Double(i * 400 + 200)) }),
                      ],
                      yAxisLabel: "tokens",
                      granularity: .hourly,
                      xDomain: dates.first!...dates.last!
                  )
                  .frame(width: 300)
                  .padding()
              ), as: .image)
          }
      }

      @Test func lineWidgetView_singleSeries() {
          let dates = lineSampleDates
          withSnapshotTesting(record: record) {
              assertSnapshot(of: snapshot(
                  LineWidgetView(
                      title: "Cost Over Time",
                      series: [
                          LineSeries(label: "Cost", color: .orange,
                                     dataPoints: dates.enumerated().map { i, d in (date: d, value: Double(i) * 0.012) }),
                      ],
                      yAxisLabel: "USD",
                      granularity: .hourly,
                      xDomain: dates.first!...dates.last!
                  )
                  .frame(width: 300)
                  .padding()
              ), as: .image)
          }
      }

      @Test func lineWidgetView_withGaps() {
          let dates = lineSampleDates
          let gapped: [(date: Date, value: Double)] = dates.enumerated().compactMap { i, d in
              guard !(0...3).contains(i), !(10...13).contains(i), !(20...23).contains(i) else { return nil }
              return (date: d, value: Double(i * 1200 + 800))
          }
          withSnapshotTesting(record: record) {
              assertSnapshot(of: snapshot(
                  LineWidgetView(
                      title: "Tokens Over Time",
                      series: [LineSeries(label: "Input Tokens", color: .blue, dataPoints: gapped)],
                      yAxisLabel: "tokens",
                      granularity: .hourly,
                      xDomain: dates.first!...dates.last!
                  )
                  .frame(width: 300)
                  .padding()
              ), as: .image)
          }
      }

      @Test func lineWidgetView_loading() {
          let dates = lineSampleDates
          withSnapshotTesting(record: record) {
              assertSnapshot(of: snapshot(
                  LineWidgetView(
                      title: "Tokens Over Time",
                      series: [],
                      yAxisLabel: "tokens",
                      granularity: .hourly,
                      xDomain: dates.first!...dates.last!
                  )
                  .redacted(reason: .placeholder)
                  .frame(width: 300)
                  .padding()
              ), as: .image)
          }
      }

      // MARK: - BarWidgetView

      @Test func barWidgetView_default() {
          withSnapshotTesting(record: record) {
              assertSnapshot(of: snapshot(
                  BarWidgetView(
                      title: "Tool Usage",
                      items: [
                          (name: "bash",  count: 42),
                          (name: "read",  count: 38),
                          (name: "write", count: 21),
                          (name: "edit",  count: 17),
                          (name: "grep",  count: 9),
                      ]
                  )
                  .frame(width: 220)
                  .padding()
              ), as: .image)
          }
      }

      @Test func barWidgetView_empty() {
          withSnapshotTesting(record: record) {
              assertSnapshot(of: snapshot(
                  BarWidgetView(title: "Tool Usage", items: [])
                      .frame(width: 220)
                      .padding()
              ), as: .image)
          }
      }

      @Test func barWidgetView_loading() {
          withSnapshotTesting(record: record) {
              assertSnapshot(of: snapshot(
                  BarWidgetView(title: "Tool Usage", items: [])
                      .redacted(reason: .placeholder)
                      .frame(width: 220)
                      .padding()
              ), as: .image)
          }
      }

      // MARK: - StatWidgetView

      @Test func statWidgetView_default() {
          withSnapshotTesting(record: record) {
              assertSnapshot(of: snapshot(
                  StatWidgetView(
                      title: "Total Tokens",
                      primaryValue: "1,234,567",
                      rows: [
                          (label: "Input",      value: "800,000"),
                          (label: "Output",     value: "400,000"),
                          (label: "Cache Read", value: "34,567"),
                      ]
                  )
                  .frame(width: 220)
                  .padding()
              ), as: .image)
          }
      }

      @Test func statWidgetView_emptyRows() {
          withSnapshotTesting(record: record) {
              assertSnapshot(of: snapshot(
                  StatWidgetView(title: "Total Cost", primaryValue: "$12.34", rows: [])
                      .frame(width: 220)
                      .padding()
              ), as: .image)
          }
      }

      @Test func statWidgetView_loading() {
          withSnapshotTesting(record: record) {
              assertSnapshot(of: snapshot(
                  StatWidgetView(
                      title: "Total Tokens",
                      primaryValue: "000,000",
                      rows: [
                          (label: "Input",      value: "000,000"),
                          (label: "Output",     value: "000,000"),
                          (label: "Cache Read", value: "000,000"),
                      ]
                  )
                  .redacted(reason: .placeholder)
                  .frame(width: 220)
                  .padding()
              ), as: .image)
          }
      }

      // MARK: - HeatmapWidgetView

      // Use fixed counts instead of Int.random(in:) to keep snapshots deterministic.
      private var heatmapFixedEntries: [(weekday: Int, hour: Int, count: Int)] {
          let counts = [3, 7, 2, 8, 5, 1, 9, 4, 6, 10]
          var entries: [(weekday: Int, hour: Int, count: Int)] = []
          var i = 0
          for wd in 2...6 {
              for hr in 9...17 {
                  entries.append((weekday: wd, hour: hr, count: counts[i % counts.count]))
                  i += 1
              }
          }
          entries.append((weekday: 1, hour: 20, count: 3))
          return entries
      }

      @Test func heatmapWidgetView_monFirst() {
          withSnapshotTesting(record: record) {
              assertSnapshot(of: snapshot(
                  HeatmapWidgetView(title: "Usage by Time", data: heatmapFixedEntries)
                      .frame(width: 300)
                      .padding()
              ), as: .image)
          }
      }

      @Test func heatmapWidgetView_sunFirst() {
          let store = UserDefaults(suiteName: "vrt.preview.sunFirst")!
          store.set(1, forKey: "weekStartDay")
          defer { store.removePersistentDomain(forName: "vrt.preview.sunFirst") }
          withSnapshotTesting(record: record) {
              assertSnapshot(of: snapshot(
                  HeatmapWidgetView(title: "Usage by Time", data: heatmapFixedEntries)
                      .defaultAppStorage(store)
                      .frame(width: 300)
                      .padding()
              ), as: .image)
          }
      }

      @Test func heatmapWidgetView_empty() {
          withSnapshotTesting(record: record) {
              assertSnapshot(of: snapshot(
                  HeatmapWidgetView(title: "Usage by Time", data: [])
                      .frame(width: 300)
                      .padding()
              ), as: .image)
          }
      }

      @Test func heatmapWidgetView_loading() {
          withSnapshotTesting(record: record) {
              assertSnapshot(of: snapshot(
                  HeatmapWidgetView(title: "Usage by Time", data: [])
                      .redacted(reason: .placeholder)
                      .frame(width: 300)
                      .padding()
              ), as: .image)
          }
      }

      // MARK: - SetupGuideView

      @Test func setupGuideView_default() {
          withSnapshotTesting(record: record) {
              assertSnapshot(of: snapshot(
                  SetupGuideView()
                      .environment(AppSettings())
                      .environment(OTLPServer())
              ), as: .image)
          }
      }

      // MARK: - SettingsView

      @Test func settingsView_default() {
          withSnapshotTesting(record: record) {
              assertSnapshot(of: snapshot(
                  SettingsView()
                      .environment(AppSettings())
                      .environment(OTLPServer())
              ), as: .image)
          }
      }

      // MARK: - GanttChartView

      @Test func ganttChartView_default() {
          let spans = [
              OTLPSpan(traceId: "t1", spanId: "s1", name: "root", kind: .server,
                       startTime: referenceDate, endTime: referenceDate.addingTimeInterval(0.5),
                       status: .ok),
              OTLPSpan(traceId: "t1", spanId: "s2", parentSpanId: "s1", name: "child-1",
                       kind: .internal, startTime: referenceDate.addingTimeInterval(0.05),
                       endTime: referenceDate.addingTimeInterval(0.2)),
              OTLPSpan(traceId: "t1", spanId: "s3", parentSpanId: "s1", name: "child-2",
                       kind: .client, startTime: referenceDate.addingTimeInterval(0.25),
                       endTime: referenceDate.addingTimeInterval(0.45), status: .error),
          ]
          withSnapshotTesting(record: record) {
              assertSnapshot(of: snapshot(
                  GanttChartView(spans: spans)
                      .frame(width: 600, height: 300)
              ), as: .image)
          }
      }

      // MARK: - TraceListView

      @Test func traceListView_default() throws {
          let container = try ModelContainer(
              for: ResourceSpans.self, ScopeSpans.self, OTLPSpan.self, SpanAttribute.self,
                  ResourceAttribute.self, ResourceMetrics.self, ResourceLogs.self,
              configurations: ModelConfiguration(isStoredInMemoryOnly: true)
          )
          withSnapshotTesting(record: record) {
              assertSnapshot(of: snapshot(
                  TraceListView().modelContainer(container)
              ), as: .image)
          }
      }

      // MARK: - MetricsView

      @Test func metricsView_default() throws {
          let container = try ModelContainer(
              for: ResourceSpans.self, ScopeSpans.self, OTLPSpan.self, SpanAttribute.self,
                  ResourceAttribute.self, ResourceMetrics.self, ResourceLogs.self,
              configurations: ModelConfiguration(isStoredInMemoryOnly: true)
          )
          withSnapshotTesting(record: record) {
              assertSnapshot(of: snapshot(
                  MetricsView().modelContainer(container)
              ), as: .image)
          }
      }
  }
  ```

- [ ] **Step 2: テストを記録モードで実行してスナップショットが生成されることを確認**

  ```bash
  export RECORD_SNAPSHOTS=true
  xcodebuild -scheme Teloscope test \
    -only-testing TeloscopeTests/PreviewSnapshotTests \
    2>&1 | xcbeautify
  ```

  Expected: `TEST SUCCEEDED`、`TeloscopeTests/VRT/__Snapshots__/PreviewSnapshotTests/` に `.png` ファイルが生成される。

  > Note: `fittingSize` がゼロを返すビューがある場合は、そのビューに明示的な `CGSize` を渡すよう `snapshot()` のフレームを調整する。

- [ ] **Step 3: 比較モードで実行してテストが通ることを確認**

  ```bash
  xcodebuild -scheme Teloscope test \
    -only-testing TeloscopeTests/PreviewSnapshotTests \
    2>&1 | xcbeautify
  ```

  Expected: `TEST SUCCEEDED`（baseline と新規 PNG が一致）

- [ ] **Step 4: コミット**

  ```bash
  git add TeloscopeTests/VRT/PreviewSnapshotTests.swift
  git commit -m "test: add VRT snapshot tests for all SwiftUI previews"
  ```

---

## Task 4: 初期 baseline を vrt-baselines ブランチに push

**Files:** なし（Git 操作のみ）

- [ ] **Step 1: 生成済みスナップショットを vrt-baselines ブランチに push**

  ```bash
  SNAPSHOT_SRC="TeloscopeTests/VRT/__Snapshots__/PreviewSnapshotTests"

  git config user.email "dev@local"
  git config user.name "Developer"

  git worktree add /tmp/vrt-baselines vrt-baselines
  mkdir -p "/tmp/vrt-baselines/TeloscopeTests/VRT/__Snapshots__"
  rsync -av --delete "$SNAPSHOT_SRC/" \
    "/tmp/vrt-baselines/TeloscopeTests/VRT/__Snapshots__/PreviewSnapshotTests/"

  cd /tmp/vrt-baselines
  git add -A
  git commit -m "chore: add initial VRT baselines"
  git push origin vrt-baselines
  cd -
  git worktree remove /tmp/vrt-baselines
  ```

  Expected: GitHub の `vrt-baselines` ブランチに PNG ファイルが存在する。

---

## Task 5: vrt-report.py を作成

**Files:**
- Create: `scripts/vrt-report.py`

- [ ] **Step 1: スクリプトを作成**

  ```python
  #!/usr/bin/env python3
  # SPDX-License-Identifier: MIT
  """
  Compare baseline PNGs against newly-generated snapshots.
  Creates 3-panel diff images (baseline | diff | new) and writes
  a GitHub Actions Job Summary. Exits with code 1 if any view
  exceeds the change threshold.
  """
  import base64
  import io
  import os
  import sys
  from pathlib import Path

  from PIL import Image, ImageChops, ImageDraw

  THRESHOLD = 0.01  # fail if more than 1% of pixels change


  def pixel_diff_ratio(baseline: Image.Image, new: Image.Image) -> float:
      if baseline.size != new.size:
          new = new.resize(baseline.size, Image.LANCZOS)
      diff = ImageChops.difference(baseline.convert("RGB"), new.convert("RGB"))
      changed = sum(1 for r, g, b in diff.getdata() if max(r, g, b) > 10)
      return changed / (baseline.width * baseline.height)


  def create_panel(baseline: Image.Image, new: Image.Image) -> Image.Image:
      if baseline.size != new.size:
          new = new.resize(baseline.size, Image.LANCZOS)

      diff_rgb = ImageChops.difference(baseline.convert("RGB"), new.convert("RGB"))

      # Red-highlight changed pixels on a copy of the baseline
      highlighted = baseline.convert("RGBA").copy()
      diff_px = diff_rgb.load()
      hi_px = highlighted.load()
      for y in range(diff_rgb.height):
          for x in range(diff_rgb.width):
              if max(diff_px[x, y]) > 10:
                  hi_px[x, y] = (220, 50, 50, 230)

      gap = 6
      label_h = 18
      w, h = baseline.width, baseline.height
      total_w = w * 3 + gap * 4
      total_h = h + label_h + gap * 2

      panel = Image.new("RGB", (total_w, total_h), (245, 245, 245))
      draw = ImageDraw.Draw(panel)

      labels = ["baseline", "diff", "new"]
      images = [baseline.convert("RGB"), highlighted.convert("RGB"), new.convert("RGB")]
      for i, (label, img) in enumerate(zip(labels, images)):
          x = gap + i * (w + gap)
          draw.text((x + 4, 3), label, fill=(100, 100, 100))
          panel.paste(img, (x, label_h + gap))

      return panel


  def to_data_uri(img: Image.Image) -> str:
      buf = io.BytesIO()
      img.save(buf, format="PNG")
      return "data:image/png;base64," + base64.b64encode(buf.getvalue()).decode()


  def main() -> int:
      if len(sys.argv) != 3:
          print("Usage: vrt-report.py <baseline_dir> <new_dir>", file=sys.stderr)
          return 2

      baseline_dir = Path(sys.argv[1])
      new_dir = Path(sys.argv[2])
      summary_path = os.environ.get("GITHUB_STEP_SUMMARY")

      failures: list[tuple[Path, float, Image.Image, Image.Image]] = []

      for baseline_path in sorted(baseline_dir.rglob("*.png")):
          rel = baseline_path.relative_to(baseline_dir)
          new_path = new_dir / rel
          if not new_path.exists():
              print(f"WARNING: no new snapshot for {rel}", file=sys.stderr)
              continue
          baseline_img = Image.open(baseline_path)
          new_img = Image.open(new_path)
          ratio = pixel_diff_ratio(baseline_img, new_img)
          if ratio > THRESHOLD:
              failures.append((rel, ratio, baseline_img, new_img))

      lines: list[str] = []
      if failures:
          lines.append(f"## VRT Results — {len(failures)} difference(s) found\n")
          for rel, ratio, baseline_img, new_img in failures:
              panel = create_panel(baseline_img, new_img)
              lines.append(f"### {rel.stem} ({ratio * 100:.1f}% changed) ❌")
              lines.append(f"![diff]({to_data_uri(panel)})\n")
          lines.append("---")
          lines.append("To approve: add label `vrt-approved` to this PR")
      else:
          lines.append("## VRT Results — All passed ✅")

      output = "\n".join(lines)
      print(output)
      if summary_path:
          with open(summary_path, "a") as f:
              f.write(output + "\n")

      return 1 if failures else 0


  if __name__ == "__main__":
      sys.exit(main())
  ```

- [ ] **Step 2: 実行権限を付与**

  ```bash
  chmod +x scripts/vrt-report.py
  ```

- [ ] **Step 3: ローカルで動作確認**

  ```bash
  pip3 install Pillow

  # テスト: 同じ画像を比較 → pass
  python3 scripts/vrt-report.py \
    "TeloscopeTests/VRT/__Snapshots__/PreviewSnapshotTests" \
    "TeloscopeTests/VRT/__Snapshots__/PreviewSnapshotTests"
  echo "Exit code: $?"
  ```

  Expected: `## VRT Results — All passed ✅`、exit code 0

- [ ] **Step 4: コミット**

  ```bash
  git add scripts/vrt-report.py
  git commit -m "feat: add VRT diff report script"
  ```

---

## Task 6: .github/workflows/vrt.yml を作成

**Files:**
- Create: `.github/workflows/vrt.yml`

- [ ] **Step 1: ワークフローを作成**

  ```yaml
  name: vrt

  on:
    push:
      branches: [main]
      paths-ignore:
        - '*.md'
    pull_request:
      branches: [main]
      paths-ignore:
        - '*.md'

  defaults:
    run:
      shell: bash

  jobs:
    # PR 時: baseline と比較して差分を Job Summary に表示
    vrt-check:
      if: github.event_name == 'pull_request'
      runs-on: macos-26
      steps:
        - name: Select Xcode version
          run: sudo xcode-select -s '/Applications/Xcode_26.4.app/Contents/Developer'

        - uses: actions/checkout@v6

        - name: Fetch baselines from vrt-baselines
          run: |
            git fetch origin vrt-baselines
            git checkout origin/vrt-baselines -- "TeloscopeTests/VRT/__Snapshots__" || true
            mkdir -p Snapshots/Baseline
            if [ -d "TeloscopeTests/VRT/__Snapshots__" ]; then
              rsync -a "TeloscopeTests/VRT/__Snapshots__/" Snapshots/Baseline/
              rm -rf "TeloscopeTests/VRT/__Snapshots__"
            fi

        - name: Generate new snapshots
          run: |
            export RECORD_SNAPSHOTS=true
            xcodebuild -scheme Teloscope test \
              -only-testing TeloscopeTests/PreviewSnapshotTests \
              | xcbeautify

        - name: Install Pillow
          run: pip3 install --quiet Pillow

        - name: Run VRT report
          run: |
            python3 scripts/vrt-report.py \
              Snapshots/Baseline \
              "TeloscopeTests/VRT/__Snapshots__"

    # main push 時: 新規 baseline を生成して vrt-baselines に push
    update-baselines:
      if: github.event_name == 'push' && github.ref == 'refs/heads/main'
      runs-on: macos-26
      steps:
        - name: Select Xcode version
          run: sudo xcode-select -s '/Applications/Xcode_26.4.app/Contents/Developer'

        - uses: actions/checkout@v6
          with:
            fetch-depth: 0  # worktree 操作に必要

        - name: Generate snapshots
          run: |
            export RECORD_SNAPSHOTS=true
            xcodebuild -scheme Teloscope test \
              -only-testing TeloscopeTests/PreviewSnapshotTests \
              | xcbeautify

        - name: Push to vrt-baselines
          run: |
            SNAPSHOT_SRC="TeloscopeTests/VRT/__Snapshots__/PreviewSnapshotTests"

            git config user.name "github-actions[bot]"
            git config user.email "github-actions[bot]@users.noreply.github.com"

            git fetch origin vrt-baselines
            git worktree add /tmp/vrt-baselines origin/vrt-baselines

            mkdir -p "/tmp/vrt-baselines/TeloscopeTests/VRT/__Snapshots__"
            rsync -av --delete "$SNAPSHOT_SRC/" \
              "/tmp/vrt-baselines/TeloscopeTests/VRT/__Snapshots__/PreviewSnapshotTests/"

            cd /tmp/vrt-baselines
            git add -A
            git diff --staged --quiet || \
              git commit -m "chore: update VRT baselines from main [skip ci]"
            git push origin HEAD:vrt-baselines

            cd -
            git worktree remove /tmp/vrt-baselines --force
  ```

- [ ] **Step 2: コミット**

  ```bash
  git add .github/workflows/vrt.yml
  git commit -m "ci: add VRT workflow for PR checks and baseline auto-update"
  ```

---

## Task 7: .github/workflows/update-baseline.yml を作成

**Files:**
- Create: `.github/workflows/update-baseline.yml`

- [ ] **Step 1: ワークフローを作成**

  ```yaml
  name: update-vrt-baseline

  on:
    pull_request:
      types: [labeled]

  defaults:
    run:
      shell: bash

  jobs:
    update:
      if: github.event.label.name == 'vrt-approved'
      runs-on: macos-26
      steps:
        - name: Check maintainer write permission
          uses: actions/github-script@v7
          with:
            script: |
              const { data } = await github.rest.repos.getCollaboratorPermissionLevel({
                owner: context.repo.owner,
                repo: context.repo.repo,
                username: context.actor,
              });
              const allowed = ['admin', 'write'];
              if (!allowed.includes(data.permission)) {
                core.setFailed(
                  `@${context.actor} does not have write permission. ` +
                  `Only maintainers can approve VRT baseline updates.`
                );
              }

        - name: Select Xcode version
          run: sudo xcode-select -s '/Applications/Xcode_26.4.app/Contents/Developer'

        - uses: actions/checkout@v6
          with:
            ref: ${{ github.event.pull_request.head.sha }}
            fetch-depth: 0

        - name: Generate snapshots from PR branch
          run: |
            export RECORD_SNAPSHOTS=true
            xcodebuild -scheme Teloscope test \
              -only-testing TeloscopeTests/PreviewSnapshotTests \
              | xcbeautify

        - name: Push updated baselines to vrt-baselines
          run: |
            SNAPSHOT_SRC="TeloscopeTests/VRT/__Snapshots__/PreviewSnapshotTests"
            PR_NUM="${{ github.event.pull_request.number }}"

            git config user.name "github-actions[bot]"
            git config user.email "github-actions[bot]@users.noreply.github.com"

            git fetch origin vrt-baselines
            git worktree add /tmp/vrt-baselines origin/vrt-baselines

            mkdir -p "/tmp/vrt-baselines/TeloscopeTests/VRT/__Snapshots__"
            rsync -av --delete "$SNAPSHOT_SRC/" \
              "/tmp/vrt-baselines/TeloscopeTests/VRT/__Snapshots__/PreviewSnapshotTests/"

            cd /tmp/vrt-baselines
            git add -A
            git diff --staged --quiet || \
              git commit -m "chore: update VRT baselines from PR #${PR_NUM} [skip ci]"
            git push origin HEAD:vrt-baselines

            cd -
            git worktree remove /tmp/vrt-baselines --force
  ```

- [ ] **Step 2: GitHub に `vrt-approved` ラベルを作成**

  GitHub リポジトリの `Issues > Labels` ページを開き、`vrt-approved` ラベルを作成する（色: `#0075ca` など）。

- [ ] **Step 3: コミット**

  ```bash
  git add .github/workflows/update-baseline.yml
  git commit -m "ci: add update-baseline workflow triggered by vrt-approved label"
  ```

- [ ] **Step 4: main にプッシュ**

  ```bash
  git push origin main
  ```

---

## Task 8: エンドツーエンドのスモークテスト

**Files:** なし

- [ ] **Step 1: テスト用ブランチを作成してUIに軽微な変更を加える**

  ```bash
  git checkout -b test/vrt-smoke
  ```

  `Teloscope/Views/Metrics/PieWidgetView.swift` を開き、ピースの値を `35` → `40` に変更して保存。

- [ ] **Step 2: コミット & プッシュして PR を作成**

  ```bash
  git add Teloscope/Views/Metrics/PieWidgetView.swift
  git commit -m "test: intentional VRT change for smoke test"
  git push origin test/vrt-smoke
  gh pr create --title "test: VRT smoke test" --body "VRT スモークテスト用 PR"
  ```

- [ ] **Step 3: GitHub Actions の vrt ワークフローを確認**

  PR の Checks タブを開き、`vrt / vrt-check` ジョブが失敗することを確認。  
  ジョブをクリック → Summary タブに `PieWidgetView_default` の差分 PNG が表示されることを確認。

- [ ] **Step 4: 変更を承認して baseline を更新**

  差分が意図したものであることを確認し、`vrt-approved` ラベルを PR に付与。  
  `update-vrt-baseline` ワークフローが起動し成功することを確認。  
  その後 `vrt-check` が再実行されパスすることを確認。

- [ ] **Step 5: テスト用の変更を revert して PR をクローズ**

  ```bash
  git checkout main
  git branch -D test/vrt-smoke
  gh pr close <PR番号>
  ```

---

## 補足: `__Snapshots__` を main ブランチでは追跡しない

`TeloscopeTests/VRT/__Snapshots__/` は `vrt-baselines` ブランチで管理するため、`main` ブランチでは追跡しない。

```bash
echo "TeloscopeTests/VRT/__Snapshots__/" >> .gitignore
git add .gitignore
git commit -m "chore: ignore VRT snapshot output directory"
```
